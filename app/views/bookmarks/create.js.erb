<%# ajaxがうまく動かないのでこのファイルは消しても問題ない %>
$("#bookmark_form").html("<%= escape_javascript(render :partial => 'microposts/clear_bookmark',
                                   :locals => { :micropost => @micropost }) %>");
