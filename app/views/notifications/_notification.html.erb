<div class="notification-view">
  <%# notificationの内容を表示(helperで定義) %>
  <%= notification_form(notification) %>
  <%= "(#{time_ago_in_words(notification.created_at)}前)" %>
  <br>
  <%# <%= notificated_comment_form(notification) %>
</div>

