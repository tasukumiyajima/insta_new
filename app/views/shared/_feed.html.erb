<% if @feed_items.any? %>

  <ol class="microposts">
    <%# @feed_itemsの各要素がMicropostクラスを持っているため、RailsはMicropostのパーシャルを呼び出す %>
    <%= render @feed_items %>
  </ol>

  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>

<% end %>
